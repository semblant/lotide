/**
 * The head function returns the first item in a given array.
 *
 * @param {Array} arr - The input array.
 *
 * @returns {any} - Returns the first element of the array.
 *
 **/
const head = (arr) => {
  return arr[0];
};

module.exports = head;